# This project reads ../.env first, then overrides it with this local .env.
# Never commit real private keys or API credentials.

# Polygon Amoy testnet only (chain ID 80002).
# Optional comma-separated list. The client also has three built-in fallbacks.
AMOY_RPC_URLS=https://polygon-amoy-bor-rpc.publicnode.com,https://rpc-amoy.polygon.technology
CHAIN_ID=80002

# Runtime backend mode: research | official-v2
EXCHANGE_MODE=research
# official-v2 selects one contract from deployments/official-v2-amoy.json.
OFFICIAL_V2_DEPLOYMENT_PATH=
OFFICIAL_V2_RUNTIME_VARIANT=standard
OFFICIAL_V2_EXCHANGE_ADDRESS=
OFFICIAL_COLLATERAL_ADDRESS=
OFFICIAL_CTF_ADDRESS=
OFFICIAL_COLLATERAL_SYMBOL=pUSD
OFFICIAL_COLLATERAL_DECIMALS=6
OFFICIAL_MARKET_ID=
OFFICIAL_MARKET_CONFIG_PATH=
OFFICIAL_CONDITION_ID=
OFFICIAL_YES_TOKEN_ID=
OFFICIAL_NO_TOKEN_ID=
OFFICIAL_MARKET_QUESTION=
OFFICIAL_BUYER_WALLET=
OFFICIAL_SELLER_WALLET=
# 0=EOA, 1=官方 Proxy Wallet, 2=官方 Safe, 3=ERC-1271 合约钱包。
OFFICIAL_BUYER_SIGNATURE_TYPE=1
OFFICIAL_SELLER_SIGNATURE_TYPE=1
# 两边由不同 owner 签名时分别配置；留空则回退到 POLYMARKET_PRIVATE_KEY。
OFFICIAL_BUYER_PRIVATE_KEY=
OFFICIAL_SELLER_PRIVATE_KEY=
OFFICIAL_BUYER_PUSD_AMOUNT=10
OFFICIAL_SELLER_PUSD_AMOUNT=10
OFFICIAL_MARKET_SPLIT_AMOUNT=10

# POLYMARKET_PRIVATE_KEY takes precedence over the root ETH_PRIVATE_KEY.
# It must control the Deposit Wallet owner or an approved session signer.
POLYMARKET_PRIVATE_KEY=
DEPOSIT_WALLET=

# Polymarket does not document the production host as an Amoy order endpoint.
# Set this only if Polymarket gives you a dedicated Amoy CLOB host.
CLOB_TESTNET_API_URL=
CLOB_API_KEY=
CLOB_SECRET=
CLOB_PASS_PHRASE=

# Required only if Polymarket provides you with a dedicated Amoy relayer.
# Never put https://relayer-v2.polymarket.com here: that is Polygon production.
RELAYER_TESTNET_URL=
# Current recommended relayer authentication. RELAYER_API_KEY_ADDRESS is the
# wallet address that created/owns the key; for personal use it is normally
# the address derived from POLYMARKET_PRIVATE_KEY.
RELAYER_API_KEY=
RELAYER_API_KEY_ADDRESS=

# Legacy HMAC Builder credentials remain supported. Leave these empty when
# using RELAYER_API_KEY.
BUILDER_API_KEY=
BUILDER_SECRET=
BUILDER_PASS_PHRASE=

# Order parameters. TOKEN_ID must be selected from a current active market.
TOKEN_ID=
ORDER_SIDE=BUY
ORDER_PRICE=0.01
ORDER_SIZE=1
ORDER_TYPE=GTC
TICK_SIZE=0.01
NEG_RISK=false

# Safety switches. Read-only checks and local signing work without these.
LIVE_ACTION=NONE
# Common research values for LIVE_ACTION:
# SIMULATE_RESEARCH_MARKET, MATCH_RESEARCH_ORDERS,
# CANCEL_RESEARCH_ORDER, DEPLOY_NEW_RESEARCH_EXCHANGE,
# APPROVE_RESEARCH_EXCHANGE
LIVE_CONFIRMATION=

# Exact public CTF Exchange V2 non-production deployment.
# standard | neg-risk | all
OFFICIAL_V2_VARIANT=all
# Deployment requires LIVE_ACTION=DEPLOY_OFFICIAL_V2 and the normal
# LIVE_CONFIRMATION=AMOY_TESTNET_ONLY guard.

# Optional CTF target for the official UMA adapter. Defaults to the official
# Amoy CTF address. A Neg Risk deployment must point this at its operator.
UMA_CTF_TARGET=
# UMA deployment requires LIVE_ACTION=DEPLOY_UMA_CTF_ADAPTER.

# Optional owner used by the read-only official Proxy/Safe derivation command.
# Defaults to the address derived from POLYMARKET_PRIVATE_KEY.
OFFICIAL_WALLET_OWNER=

# Optional Amoy EIP-1559 fee cap. Check current Amoy fees before setting it.
# A transaction can remain pending when the cap is below the network minimum.
AMOY_MAX_FEE_GWEI=
AMOY_PRIORITY_FEE_GWEI=
